import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'richdad-financial-statement',
  webDir: 'dist'
};

export default config;
